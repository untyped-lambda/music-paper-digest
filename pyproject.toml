[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = [
  "E501",  # 行長超過は日本語コメント/文字列で頻発するため line-length の目安のみとし、エラーにはしない
]

[tool.ruff.lint.isort]
known-first-party = ["src"]
